#include <stdio.h>
#include <stdlib.h>

void main(){
   char letra = 'x';

   //Condicional Simples
   if(letra == 'x'){
      printf("\nA letra eh x.");
   }

   //Código respectivo à tabela ASCII
   printf("\nCodigo da letra = %d", letra);

   if(letra == 120){
      printf("\nA letra eh x.");
   }
}
